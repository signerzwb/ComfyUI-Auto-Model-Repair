# ComfyUI Workflow Doctor 1.0 开发计划

## 1. 产品定义

### 1.1 一句话定位

把从互联网获得的 ComfyUI 工作流，迁移成适配当前电脑节点、模型、版本和硬件的可运行工作流，并自动整理成从左到右、从上到下、带语义分组和说明的可读画布。

### 1.2 产品名称

- 对外显示名：`Workflow Doctor`
- 当前开发目录：`ComfyUI-Workflow-Agent`
- 版本目标：`1.0.0`
- 形态：ComfyUI 原生左侧栏扩展 + 本地 Python 后端
- 默认语言：中文
- Agent：DeepSeek，使用 OpenAI 兼容接口；保留其他兼容服务的扩展能力

### 1.3 核心价值

V1 不只是“缺什么安装什么”，而是对每个缺失依赖做以下判断：

1. 安装原始节点包；
2. 使用本机已有节点等价替代；
3. 使用多个基础节点重建原功能；
4. 对不影响主链的节点进行绕过或删除；
5. 无法安全替代时明确暂停并交给用户决定。

完成依赖修复后，再进行模型修复、结构验证、语义分组、自动排版、低成本试运行和视觉检查。

## 2. V1 范围

### 2.1 必须交付

- 读取当前画布的完整工作流 JSON、节点、连线、分组和便签；
- 读取本机实际安装的节点类型、节点输入输出结构和 ComfyUI 版本；
- 识别缺失节点、损坏连线、失效参数、缺失模型、重复加载和结构异常；
- 使用 ComfyUI-Manager 数据识别缺失节点所属项目；
- 为缺失节点生成“安装、替代、重建、绕过、暂不处理”候选方案；
- 为候选方案提供置信度、差异、风险和理由；
- 仅在用户确认后调用 ComfyUI-Manager 安装节点包；
- 安装后提示并处理 ComfyUI 重启，再次检查节点是否加载成功；
- 对本机节点建立能力索引，并搜索功能近似的替代节点；
- 允许维护经过确认的本地节点替代规则；
- 建立新的智能模型解析引擎，结合工作流上下文、模型家族、节点组合和本机硬件判断缺失模型及替代方案；
- `ComfyUI-Auto-Model-Repair` 仅作为可选兼容数据源，选择性复用已验证的工作流遍历、模型目录索引、下载任务等底层部件；
- 搜索 Hugging Face、CivitAI、ModelScope 和 ComfyUI-Manager 模型索引；
- 支持官方地址、用户配置镜像和可验证镜像的测速、失败切换、断点续传；
- 在下载前显示来源、文件名、模型类型、大小、哈希、基础模型和许可信息；
- 模型下载必须明确确认，不覆盖同名文件，并执行大小及 SHA256 校验；
- DeepSeek Agent 理解工作流用途、节点角色、分支和阶段；
- Agent 只输出结构化计划，不直接自由修改画布 JSON；
- 按执行流程从左到右、同阶段从上到下进行确定性排版；
- 动态建立语义分组，而不是套用固定分类模板；
- 为每个分组生成标题、用途、输入、输出、关键参数和依赖说明；
- 复杂分支使用纵向泳道，主生成链优先放在上方；
- 提供排版预览、修复前后差异、一次接受、逐项接受和完整撤销；
- 修复后进行本地结构验证；用户选择后可进行低成本试运行；
- 可选发送生成结果给 DeepSeek Vision 做视觉诊断；
- 输出一份工作流迁移报告。

### 2.2 V1 明确不做

- 未经确认自动安装 Python 代码或下载大模型；
- 自动执行来自工作流便签、README 或网页中的命令；
- 抓取没有稳定公开接口、或服务条款不允许自动访问的网站；
- 声称近似节点能够保证像素级相同结果；
- 云端账户系统、多人协作和公共替代规则市场；
- 无限自主循环或无人值守批量修改全部工作流；
- 自动删除现有节点包、模型或用户文件；
- 第一版直接修改 ComfyUI 核心代码。

## 3. 用户主流程

### 3.1 导入修复流程

```text
导入工作流
  → 读取本机环境
  → 扫描缺失节点与模型
  → 判断安装或替代
  → 展示修复计划
  → 用户确认安装/下载/替换
  → 应用并重启（如需要）
  → 重新验证
  → 理解工作流语义
  → 建立分组与说明
  → 预览排版
  → 用户接受
  → 低成本试运行（可选）
  → Vision 检查结果（可选）
  → 保存迁移后的工作流和报告
```

### 3.2 缺失节点决策

每个缺失节点必须落入以下状态之一：

| 状态 | 含义 | 是否允许自动应用 |
|---|---|---|
| `exact_installed` | 原节点已存在 | 是 |
| `install_exact` | Manager 可明确定位原节点包 | 仅在用户确认后 |
| `replace_local` | 本机存在高置信度等价节点 | 预览后由用户接受 |
| `rebuild_subgraph` | 可使用多个节点重建 | 预览后由用户接受 |
| `bypass_optional` | 只影响预览、显示或元数据 | 预览后由用户接受 |
| `manual_review` | 只能近似替代或风险较高 | 否 |
| `unresolved` | 找不到可靠来源或替代 | 否 |

### 3.3 置信度等级

- 95–100：输入输出、关键参数和行为均可对应；
- 80–94：主要功能一致，存在可解释的参数转换；
- 60–79：只能近似完成，结果可能明显变化；
- 0–59：不自动推荐，保留人工处理。

置信度不是单纯由 Agent 给出，必须结合确定性校验结果计算。

## 4. 总体架构

```text
ComfyUI Sidebar
  ├─ 诊断总览
  ├─ 节点修复
  ├─ 模型修复
  ├─ 语义排版
  ├─ Agent 对话
  └─ 历史与撤销
        │
        ▼
Workflow Doctor Backend
  ├─ Environment Inspector
  ├─ Workflow Graph Analyzer
  ├─ Node Package Resolver
  ├─ Capability & Substitution Engine
  ├─ Intelligent Model Resolver
  ├─ Model Search & Mirror Router
  ├─ DeepSeek Planner
  ├─ Patch Validator
  ├─ Semantic Layout Engine
  └─ Snapshot / Report Store
        │
        ├─ ComfyUI live schemas / object_info
        ├─ ComfyUI-Manager databases and install API
        ├─ ComfyUI-Auto-Model-Repair optional adapter
        ├─ Hugging Face / CivitAI / ModelScope
        └─ DeepSeek API
```

## 5. 模块设计

### 5.1 Environment Inspector

职责：生成本机环境事实快照。

收集：

- ComfyUI 版本；
- Python、PyTorch、CUDA 和 GPU/VRAM 信息；
- 已加载节点类型；
- 每种节点的输入、输出、控件和默认值；
- 已安装自定义节点包及版本；
- ComfyUI-Manager 是否存在及其能力；
- Auto-Model-Repair 是否存在、其 API 版本以及哪些底层能力可以安全复用；
- 模型目录、可用空间和模型索引；
- 当前工作流使用的节点和模型。

要求：

- 环境快照可缓存；
- 重启后可刷新；
- 发送给 Agent 前移除本地绝对路径、密钥和无关隐私信息。

### 5.2 Workflow Graph Analyzer

职责：将 ComfyUI 工作流转换为稳定的内部图模型。

能力：

- 同时兼容旧数组 links 和新版对象 links；
- 识别源节点、汇点、主输出、分支、汇合点、孤立节点和循环异常；
- 计算拓扑层级和执行方向；
- 识别 UI 格式与 API 格式；
- 保留子图、分组、Reroute、节点尺寸和现有说明；
- 记录损坏链接、未知节点和无效 widget 值；
- 对大工作流生成按子图切片的摘要，避免一次将全部 JSON 发送给 Agent。

### 5.3 Node Package Resolver

职责：回答“缺失节点来自哪个项目，是否应该安装”。

数据优先级：

1. 工作流节点属性中的 `cnr_id`、`aux_id`、版本和仓库元数据；
2. ComfyUI-Manager 的 `extension-node-map.json`；
3. ComfyUI-Manager 的 `custom-node-list.json`；
4. Comfy Registry 可用信息；
5. Agent 对候选项目文档的辅助判断。

每个候选包返回：

- 节点包 ID、名称、仓库地址和版本；
- 能否由 Manager 安装；
- 是否已经安装；
- 最近可用性信息（如数据源提供）；
- 需要重启与否；
- 风险提示；
- 命中的缺失节点列表。

安装规则：

- 优先调用 ComfyUI-Manager 的受控安装接口；
- 不从 Agent 返回的任意 URL 直接执行安装；
- 安装前列出将安装的包和依赖；
- 每次安装必须由用户确认；
- 安装后等待用户重启或调用已有安全重启能力；
- 重启后重新读取实际节点类型，不能只相信安装返回成功。

### 5.4 Capability & Substitution Engine

职责：回答“原节点是否必须安装，能否由其他节点完成”。

为节点建立标准能力描述：

- 功能类别与自然语言说明；
- 输入端口类型、是否必需、数量；
- 输出端口类型和数量；
- widget 参数、范围和默认值；
- 对尺寸、随机性、颜色空间、采样、模型状态的影响；
- 是否是纯辅助节点；
- 是否需要外部模型或服务；
- 所属节点包和版本。

候选召回分四层：

1. 本地已确认替代规则；
2. 输入输出结构精确或兼容匹配；
3. 名称、类别、文档和邻接节点的语义匹配；
4. DeepSeek 对候选进行上下文排序和差异解释。

确定性校验必须检查：

- 输入输出类型兼容；
- 多输出索引是否对应；
- 必需参数是否可映射；
- 原有上下游连线能否保持；
- 是否引入新的模型、节点包或云端依赖；
- 是否改变工作流的主要媒体类型和尺寸；
- 替换后能否通过 ComfyUI schema 验证。

替代知识库存储：

- V1 使用版本化 JSON；
- 内置规则和用户确认规则分开保存；
- 每条规则包含来源节点、目标节点或子图、参数映射、端口映射、适用版本、已验证次数和最后验证结果；
- 用户可以禁用或删除自己的规则；
- 不自动上传。

### 5.5 Intelligent Model Resolver

职责：独立完成模型需求理解、搜索、兼容性判断、替代推荐和下载后的工作流修复。`ComfyUI-Auto-Model-Repair` 不是核心依赖。

#### 为什么不能直接复用原有解析逻辑

原有 Auto-Model-Repair 主要依赖文件名、节点名称、widget 位置、规则表和字符串模糊度，适合确定性的路径修复和候选召回，但不足以处理：

- 文件名完全不同但功能等价的模型；
- 同一模型的不同精度、量化、剪枝和格式；
- SD1.5、SDXL、Flux、Wan、LTX、Hunyuan、Qwen 等模型家族兼容性；
- 一个工作流阶段同时缺少 diffusion model、text encoder、VAE 等整套依赖；
- 替换模型后需要同步更换加载节点、提示词编码器或采样参数；
- 根据显存、磁盘、速度和画质在多个候选之间权衡；
- 从工作流上下游关系推断一个未知 widget 实际代表哪种模型；
- 搜索结果是否真正适用于当前工作流，而不是名字看起来相似。

#### 新的智能解析流程

1. 从 live node schema 确定 widget 的真实名称、类型和允许值；
2. 结合节点类型、节点包、上下游连接和所在阶段判断模型角色；
3. 识别模型家族、用途、精度、量化、格式和配套文件；
4. 将单个文件需求提升为“模型依赖包”，避免只下载主模型却遗漏编码器或 VAE；
5. 扫描本机模型，并先查找严格兼容的已有替代；
6. 根据缺失文件名、元数据、工作流说明和上下文生成多组搜索条件；
7. 从多个来源召回候选并规范化为统一数据结构；
8. 使用硬性规则排除架构、类型、格式和资源条件不兼容的候选；
9. 由 DeepSeek 对剩余候选进行语义排序并解释差异；
10. 本地校验器复核 Agent 结论，生成“本地替代 / 下载原版 / 下载兼容替代 / 整套迁移 / 人工判断”计划；
11. 用户确认后下载并校验；
12. 更新工作流中的模型值，必要时与节点替代引擎联合生成 patch；
13. 重新执行 schema 验证和低成本试运行。

#### 模型候选统一结构

每个候选至少包含：

- 模型身份、来源和版本；
- 文件名、文件角色和目标目录；
- 基础模型家族；
- 类型：checkpoint、diffusion model、LoRA、VAE、text encoder、ControlNet、CLIP Vision、upscaler 等；
- 精度、量化和文件格式；
- 文件大小、哈希和许可；
- gated、NSFW 和登录要求；
- 本机显存、内存和磁盘适配情况；
- 与原需求的兼容分数；
- 替换后需要联动修改的节点、参数和其他模型；
- 证据来源与尚未验证的假设。

#### 与 Auto-Model-Repair 的关系

可以选择性复用的内容：

- 已验证正确的工作流节点遍历和 widget 写回工具；
- ComfyUI 模型目录枚举和现有文件索引；
- 工作流便签中的直接链接提取；
- 已经过安全审计的下载任务、进度、取消和断点续传能力；
- 用户过去确认过的文件名路径修复结果。

不直接复用的内容：

- 仅依赖节点名和文件名的模型类型判断；
- 仅依据字符串相似度的最终替代决策；
- 固定阈值自动应用逻辑；
- 把工作流便签链接视为可信下载结论；
- 缺乏模型家族和整套依赖判断的搜索排序。

V1 实现独立的智能模型引擎，同时提供 Auto-Model-Repair 兼容适配器。未安装 Auto-Model-Repair 时，Workflow Doctor 的模型分析、搜索、推荐和下载仍需完整可用；安装后可复用其合适的底层能力和历史数据，但最终决策始终由新的解析与校验流程完成。

### 5.6 Model Search & Mirror Router

内置搜索来源：

- Hugging Face；
- CivitAI；
- ModelScope；
- ComfyUI-Manager 模型索引；
- 工作流 `models` 元数据和便签链接。

镜像策略：

1. 先从权威来源获得模型身份、版本、文件名、大小、哈希和许可；
2. 为同一文件建立多个候选下载地址；
3. 使用轻量请求检测连通性、响应时间、Range 支持和文件大小；
4. 优先选择用户配置的国内镜像；
5. 镜像失败自动切换到下一个已验证来源；
6. 支持断点续传；
7. 最终文件必须通过大小和哈希校验；
8. 校验失败删除临时文件，不替换正式模型；
9. 没有权威哈希时明确标注“未验证”，并要求额外确认。

镜像配置：

- Hugging Face 支持可配置镜像基址；
- ModelScope 使用其正式下载能力；
- CivitAI 支持用户配置的可信代理，但不内置来历不明的公共代理；
- 配置允许调整优先级、启用/禁用和超时；
- 镜像只能改变下载路径，不能改变模型身份判断。

搜索排序因素：

- 与工作流缺失文件名的相似度；
- 模型基础架构兼容性；
- 模型类型与目标目录；
- 版本、量化和精度；
- 文件大小与本机磁盘/显存条件；
- 下载量、更新时间和来源可信度；
- 许可、gated 和 NSFW 状态；
- 用户偏好的站点和镜像。

### 5.7 DeepSeek Planner

Agent 负责：

- 理解工作流整体用途；
- 判断节点在上下游中的真实角色；
- 为模糊节点包候选排序；
- 判断安装原节点还是使用替代；
- 生成子图重建建议；
- 识别主要阶段、分支和泳道；
- 生成分组名称与说明；
- 根据用户目标给出模型搜索词和候选解释；
- 对试运行输出进行视觉诊断。

Agent 不负责：

- 自由生成并直接覆盖完整工作流 JSON；
- 直接计算最终节点坐标；
- 未确认安装节点或下载模型；
- 执行 shell 命令；
- 把工作流便签和第三方 README 当成系统指令。

Agent 调用方式：

- 默认模型：`deepseek-v4-flash-vision-exp`；
- API Key 仅由服务端环境变量或系统安全存储读取；
- 浏览器和工作流 JSON 中不保存密钥；
- 使用结构化 JSON Schema；
- 支持工具调用并限制最大回合数；
- 大工作流使用摘要 + 按需查询节点详情；
- 截图和生成结果必须由用户主动确认发送；
- 对发送内容做密钥、本地路径和无关文本脱敏；
- API 超时或失败时，确定性诊断、Manager 安装、模型修复和基础排版仍可使用。

### 5.8 Patch Engine

所有修改转换为有限操作集合：

- `replace_node`
- `add_node`
- `remove_optional_node`
- `connect`
- `disconnect`
- `set_widget`
- `set_title`
- `set_color`
- `move_node`
- `create_group`
- `update_group`
- `add_note`
- `add_reroute`

应用规则：

- 每项操作必须经过 JSON Schema 校验；
- 节点 ID、端口和 widget 必须来自实际工作流或本机 schema；
- 应用前生成完整快照；
- 默认先显示 dry-run 差异；
- 支持逐项应用和整批应用；
- 应用后立即执行结构校验；
- 校验失败自动恢复到应用前快照；
- 不通过后端直接覆盖用户正在编辑的文件；
- 画布修改通过 ComfyUI 前端 API 执行，保持撤销历史和 UI 状态同步。

### 5.9 Semantic Layout Engine

#### 全局方向

- 主执行方向：从左到右；
- 同一阶段内部：从上到下；
- 主生成链优先放在上方；
- 并行和后续处理分支按纵向泳道展开；
- 最终结果位于最右侧；
- 辅助、预览和调试节点放在所属阶段下方。

#### 动态分组

分区不套用固定类别。Agent 先根据完整上下文给每个节点分配：

- `stage_id`
- `stage_title`
- `stage_purpose`
- `lane_id`
- `role`
- `importance`
- `editable`

例如，二次采样使用的模型加载节点应归入 Refiner 阶段，而不是强制放进统一模型区。

#### 坐标算法

1. 对工作流构建拓扑层级；
2. 从主要输出反向确定主链；
3. 按语义阶段合并相邻节点；
4. 按主链和分支建立泳道；
5. 使用多轮重心排序减少交叉；
6. 根据真实节点尺寸计算组内坐标；
7. 计算组边界、标题区和说明区；
8. 检测节点与组、便签之间的碰撞；
9. 对跨组长连线生成边界 Reroute 计划；
10. 再次检查重叠、逆向连线和整体宽高。

Agent 只决定语义，不决定像素坐标，保证相同输入和配置得到稳定结果。

#### 分组说明模板

```text
阶段名称

作用：这一段完成什么任务。
输入：来自哪些阶段的主要数据。
输出：交给哪个后续阶段。
重点参数：用户最可能调整的参数。
依赖：所需模型、节点包或外部服务。
```

画布便签使用简短版本，完整说明显示在侧栏。

#### 布局模式

- `structure_only`：只整理位置，不新增分组；
- `preserve_groups`：保留现有分组并优化；
- `semantic_rebuild`：重新理解、分组、说明和排版；
- `selection_only`：只处理用户选中的子图。

默认模式：`semantic_rebuild`。

### 5.10 Snapshot、历史与迁移报告

每次操作保存：

- 原工作流快照；
- 修复计划；
- 用户批准的安装、下载和替换；
- 应用后的工作流；
- 验证结果；
- 使用的模型和节点包版本；
- Agent 模型名称，但不保存 API Key；
- 试运行输出和视觉诊断引用；
- 时间与插件版本。

迁移报告至少包含：

- 原始问题；
- 安装的节点包；
- 替换或重建的节点；
- 未解决项；
- 修复的模型路径和下载模型；
- 排版分组摘要；
- 验证和试运行结果；
- 与原工作流可能存在的行为差异。

## 6. UI 设计

### 6.1 左侧栏页面

#### 总览

- 当前工作流名称；
- 可运行状态：可运行 / 需修复 / 等待重启 / 需人工判断；
- 节点、模型、连线、分组统计；
- “开始检查”主按钮；
- 推荐的下一步操作。

#### 节点修复

- 按缺失节点归类；
- 每个节点展示原包安装、现有替代、子图重建和忽略选项；
- 显示置信度、行为差异和风险；
- 支持逐项确认；
- 安装动作单独确认。

#### 模型修复

- 使用新的智能模型解析结果；
- 展示模型角色、家族、配套依赖和本机适配情况；
- 本地精确匹配、兼容替代、整套迁移和在线搜索；
- 可选导入 Auto-Model-Repair 的底层扫描信息和历史路径修复；
- 显示官方源和镜像测速；
- 下载任务、速度、进度、暂停、继续和取消；
- 下载后自动重新扫描。

#### 语义排版

- 模式选择；
- 显示识别到的阶段和泳道；
- 可修改分组名称和说明；
- 预览、接受、撤销；
- 可配置横向间距、纵向间距和分组留白。

#### Agent

- 用自然语言解释或调整修复计划；
- 示例：“这个节点不要安装，优先找本机替代”；
- 示例：“把高清修复放在第二条泳道”；
- 所有会改变画布的建议都先进入计划区。

#### 历史

- 最近快照；
- 查看修改摘要；
- 恢复；
- 导出迁移报告。

### 6.2 交互原则

- 面向非技术用户，不暴露不必要的 JSON 和命令；
- 结论先于技术细节；
- 安装、下载、替换三类动作使用不同视觉标识；
- 高风险候选不能放在默认主按钮；
- 每个错误都给出下一步，而不是只报告失败；
- Agent 离线时明确降级，不让整个插件不可用。

## 7. 后端 API 草案

所有接口使用 `/workflow-doctor/v1` 前缀。

### 环境与诊断

- `GET /status`
- `GET /environment`
- `POST /workflow/analyze`
- `POST /workflow/validate`

### 节点解析

- `POST /nodes/resolve-packages`
- `POST /nodes/find-substitutes`
- `GET /nodes/capabilities`
- `GET /nodes/substitution-rules`
- `POST /nodes/substitution-rules`

### Agent 计划

- `POST /agent/plan-repair`
- `POST /agent/plan-layout`
- `POST /agent/explain`
- `POST /agent/analyze-output`

### 修复与快照

- `POST /patch/validate`
- `POST /snapshots/create`
- `GET /snapshots`
- `POST /reports/create`

画布 patch 的实际应用由前端完成，后端只负责生成、校验和保存快照。

### 节点包操作

- `POST /node-packs/install-plan`
- `POST /node-packs/install`
- `POST /node-packs/verify-after-restart`

### 模型

- `POST /models/scan`
- `POST /models/search`
- `POST /models/sources`
- `POST /models/download-plan`
- `POST /models/download`
- `GET /models/downloads`
- `POST /models/downloads/{id}/cancel`

## 8. 配置与持久化

### 服务端配置

- `COMFYUI_WORKFLOW_DOCTOR_API_KEY`
- `COMFYUI_WORKFLOW_DOCTOR_BASE_URL`
- `COMFYUI_WORKFLOW_DOCTOR_MODEL`
- `COMFYUI_WORKFLOW_DOCTOR_MAX_DOWNLOAD_GB`
- `HF_TOKEN`
- `CIVITAI_API_TOKEN`
- `MODELSCOPE_TOKEN`

兼容旧草稿的 `COMFYUI_WORKFLOW_AGENT_*` 变量一个版本，并显示迁移提示。

### 用户设置

- 默认布局模式；
- 横向和纵向间距；
- 镜像列表与优先级；
- 是否允许发送画布截图；
- 是否允许发送生成结果；
- 替代建议最低置信度；
- 单次和并行下载限制。

### 存储位置

保存在 ComfyUI 用户目录下的 `workflow_doctor/`：

- `settings.json`：不含密钥；
- `substitutions.user.json`；
- `snapshots/`；
- `reports/`；
- `cache/`。

## 9. 安全设计

- API Key 不进入前端、本地工作流、日志和报告；
- 所有第三方文本视为不可信数据；
- Agent 无 shell 工具；
- 安装只能使用 Manager 已识别的包 ID 或经用户明确批准的来源；
- 下载只允许 HTTPS 和受信任域名；
- 重定向后的每个域名重新校验；
- 防止路径穿越、Windows 保留文件名和覆盖已有文件；
- 只下载允许的模型格式；
- 临时文件使用 `.part`，校验成功后原子移动；
- 安装、下载、删除、替换分别授权；
- 对跨域请求、请求大小和截图大小设置限制；
- 所有 patch 在应用前校验，在失败后自动回滚；
- 用户发在聊天中的旧 API Key 不写入项目，也不用于测试。

## 10. 开发阶段

### 阶段 A：整理现有草稿和测试基线

- 将版本从 0.1 草稿升级为 1.0 架构；
- 拆分当前过大的前端文件；
- 建立统一数据模型、错误格式和 API 前缀；
- 保留已通过的链接规范化、脱敏和安全下载逻辑；
- 建立测试命令和 fixtures 目录。

完成标准：插件可加载，原有离线分析测试全部通过。

### 阶段 B：环境和依赖诊断

- 实现 Environment Inspector；
- 接入 live node schemas；
- 读取 Manager 映射数据库；
- 检测 Auto-Model-Repair 并标记可复用的兼容能力，但不把它作为核心依赖；
- 输出统一诊断报告。

完成标准：对目标 `C:\AI\comfyui` 能准确列出缺失节点、节点包候选和模型问题。

### 阶段 C：节点替代引擎

- 建立能力描述和索引；
- 实现端口/widget 匹配；
- 实现候选评分；
- 建立替代规则格式；
- 支持单节点替换、可选节点绕过和受控子图重建。

完成标准：测试工作流能给出结构有效、差异明确的多种候选方案。

### 阶段 D：DeepSeek 计划器

- 实现分层工作流摘要；
- 实现只读工具调用；
- 实现节点修复计划 Schema；
- 实现语义阶段和泳道 Schema；
- 实现大工作流按需查询；
- 实现重试、超时、脱敏和降级。

完成标准：Agent 不能生成越权动作，所有计划均可被本地校验器接受或明确拒绝。

### 阶段 E：Patch 与撤销

- 实现有限 patch 操作；
- 实现前端 dry-run；
- 实现完整快照；
- 实现逐项/批量应用；
- 实现失败回滚和历史恢复。

完成标准：任何修复或排版都可以一键恢复到原画布。

### 阶段 F：语义分组和布局

- 实现主链、阶段、分支和泳道识别；
- 实现稳定坐标算法；
- 实现交叉减少和碰撞检测；
- 实现动态分组、标题、颜色和说明；
- 实现长连线 Reroute 计划；
- 实现四种布局模式。

完成标准：相同工作流重复整理结果稳定，没有节点重叠，主执行方向清晰。

### 阶段 G：智能模型解析、搜索和镜像

- 实现独立的工作流模型需求提取和模型家族识别；
- 实现模型依赖包、兼容替代和整套迁移计划；
- 为 Auto-Model-Repair 建立可选兼容适配器，只复用经过验证的底层能力；
- 完成 Hugging Face、CivitAI、ModelScope 和 Manager 搜索适配器；
- 实现来源聚合、兼容性排序和镜像测速；
- 实现下载队列、断点续传、校验和失败切换；
- 下载完成后自动再次扫描并应用用户确认的修复。

完成标准：官方源不可达时可切换到已配置且验证通过的镜像，最终文件校验正确。

### 阶段 H：完整 UI

- 完成六个左侧栏页面；
- 完成计划卡片、置信度、风险和差异展示；
- 完成进度、错误恢复和空状态；
- 完成中文文案和主题适配。

完成标准：用户不需要编辑 JSON 或运行命令即可完成主流程。

### 阶段 I：目标环境集成与发布

- 在隔离副本验证后再安装到 `C:\AI\comfyui\custom_nodes`；
- 使用真实大型工作流测试；
- 验证 Manager、可选 Auto-Model-Repair 适配器和当前 ComfyUI 版本兼容；
- 完成安装、配置、隐私、安全和故障排除文档；
- 更新版本为 1.0.0；
- 打包可直接复制安装的发布目录。

完成标准：通过第 12 节全部验收标准。

## 11. 测试计划

### 11.1 单元测试

- 新旧 links 格式；
- DAG、分支、汇合和异常循环；
- 缺失节点识别；
- Manager 节点到项目映射；
- 节点能力签名和候选评分；
- 端口及 widget 映射；
- patch schema 和回滚；
- 语义分组数据校验；
- 稳定布局、碰撞检测和交叉统计；
- 模型文件名、目录、URL、重定向和哈希安全；
- 镜像排序与失败切换；
- API Key 和本地路径脱敏；
- Agent 异常 JSON、超时和工具回合限制。

### 11.2 工作流 fixtures

- ComfyUI 核心最小文生图；
- 缺一个可明确安装的自定义节点包；
- 缺一个可由本机节点替代的图片处理节点；
- 缺一个可被绕过的预览节点；
- 一个需要多节点重建的封装节点；
- 缺 checkpoint、LoRA、VAE 和 text encoder；
- 双采样 / Refiner 工作流；
- ControlNet / IPAdapter 分支；
- 放大和修脸支线；
- 视频工作流；
- 已有复杂分组和便签的工作流；
- 100、300、500 节点压力样例；
- 损坏链接、重复 ID 和未知 widget；
- 包含恶意便签、危险 URL 和路径穿越文本的样例。

### 11.3 集成测试

- 插件加载和路由注册；
- ComfyUI live schema 读取；
- Manager 候选查询与受控安装计划；
- 智能模型需求提取、兼容替代和整套依赖解析；
- Auto-Model-Repair 存在与不存在两种模式；
- 浏览器画布快照、patch、撤销和重载；
- 下载暂停、恢复、取消和校验；
- DeepSeek mock 测试；
- 使用轮换后的测试 Key 进行最小真实 API 测试；
- ComfyUI 重启后的恢复和继续。

### 11.4 视觉验收

- 节点无明显重叠；
- 组标题和说明不遮挡节点；
- 主流程可从左到右阅读；
- 同阶段内部可从上到下阅读；
- 分支和汇合位置清晰；
- 输出集中在右侧；
- 辅助节点不干扰主链；
- 深色和浅色主题均可读；
- 侧栏在常用宽度下无横向溢出。

## 12. V1 验收标准

V1.0 只有在以下条件全部满足时才算完成：

1. 插件能在目标 ComfyUI 版本中无错误启动；
2. 不配置 DeepSeek 时，离线诊断、Manager 解析、确定性模型扫描和基础排版仍可使用；
3. 能准确识别测试工作流中的全部缺失节点；
4. Manager 已知节点可以生成明确的安装计划，并在确认后安装；
5. 安装后通过实际节点注册表验证，而不是只显示“安装完成”；
6. 至少支持单节点替代、可选节点绕过和规则化子图重建；
7. 所有替代都显示置信度和行为差异；
8. 低置信度替代不会自动应用；
9. 不安装 Auto-Model-Repair 时仍可完成模型扫描、搜索、推荐、下载和应用；安装后能够选择性复用其合适的底层能力；
10. 可以从至少三个模型信息来源搜索；
11. 可以在官方源和配置镜像之间切换，并支持断点续传；
12. 下载文件经过扩展名、路径、大小和哈希检查；
13. Agent 能生成有效的节点修复和语义排版计划；
14. Agent 返回的无效节点 ID、端口、参数和动作会被拒绝；
15. 复杂工作流能按从左到右、从上到下的逻辑形成动态分组和说明；
16. 排版不会改变工作流的节点参数和执行语义；
17. 修复和排版均支持预览、撤销和失败回滚；
18. 修复后的工作流能通过本机 schema 验证；
19. 用户选择后可进行试运行并查看结果；
20. Vision 分析只能在用户确认发送图片后调用；
21. API Key 不会出现在前端响应、日志、工作流和测试夹具中；
22. 能导出包含依赖、替换、模型、布局和验证结果的迁移报告；
23. 自动化测试全部通过；
24. 提供可直接复制安装的 1.0.0 发布目录和中文使用说明。

## 13. 开发期间的固定原则

- 先恢复依赖，再排版；
- 先确定性检查，再调用 Agent；
- Agent 负责理解和建议，代码负责验证和执行；
- 安装原节点与寻找替代节点同等重要；
- 分组由工作流上下文决定，不由固定节点类别决定；
- 排版结果必须稳定、可预览、可撤销；
- Auto-Model-Repair 只复用经过验证的底层部件，不继承其启发式最终决策；新的智能模型解析引擎必须可以独立工作；
- 镜像只优化下载路径，模型身份和安全校验仍以权威元数据为准；
- 任何外部代码安装、大文件下载和高风险替换都由用户确认；
- 不以“接口调用成功”代替实际工作流可运行的验证。

## 14. 开始开发后的执行规则

用户明确说“开始”后，按阶段 A 到 I 连续开发，不再重新讨论已在本文锁定的产品方向。遇到实现细节时优先采用本文原则做合理决定；只有涉及以下情况才暂停请求用户选择：

- 需要删除或覆盖用户现有文件；
- 需要安装来源不明确的代码；
- 需要改变已锁定的核心产品范围；
- 需要使用新的付费服务；
- 需要把用户数据发送到新的第三方。
